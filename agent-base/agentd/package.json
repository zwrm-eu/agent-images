{
  "name": "zwrm-agentd",
  "version": "0.13.6",
  "private": true,
  "type": "module",
  "description": "ZWRM in-VM session daemon: hosts coding-agent sessions (claude / pi / codex harness drivers) behind an HTTP API",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.201",
    "@earendil-works/pi-coding-agent": "0.82.0",
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "^4.0.0"
  }
}
